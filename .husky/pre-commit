#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔄 [Logic Integrity Guard] Checking core business logic & data flows..."
node orbis-logic-guard.cjs || {
    echo "❌ Logic Mismatch Alert! Commit aborted."
    exit 1
}

echo "🔄 [Madge Guard] সার্কুলার ডিপেন্ডেন্সি চেক..."
npm run check:circular || {
    echo "❌ Circular Dependency Alert!"
    exit 1
}

echo "✅ [Termux] Read-only pre-commit verification passed!"
