# ORBIS Termux / Android / Offline Intelligence Observatory

Progress is read from docs/AUDIT_REPORTS. The dashboard does not hardcode TASK completion.

Workflow: implement -> verify -> audit -> commit -> push. Future audited tasks appear automatically.

This is an observation layer and does not execute Termux commands or grant root access.
